import { EmailvalidatonDirective } from './emailvalidaton.directive';

describe('EmailvalidatonDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailvalidatonDirective();
    expect(directive).toBeTruthy();
  });
});
